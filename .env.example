# Setu Captions needs NO environment variables and NO API keys for its
# core (free) workflow: transcription runs entirely in the browser using a
# locally-downloaded, open-source Whisper model via @xenova/transformers.
#
# This file exists only as a placeholder in case a future optional
# server-side feature is added. Nothing here is required to run the app.
